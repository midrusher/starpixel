import { Paths } from "../../types/api";
import { Method } from "../../util/Method";
import { ResultObject } from "../../util/ResultObject";
import { ResultArray } from "../../util/ResultArray";
export declare class GuildsResources extends Method {
    /**
     * Retrieve a list of achievements a Hypixel guild can accomplish.
     * @example
     * ```typescript
     * const achievements = await client.resources.guilds.achievements();
     * ```
     * @category API
     */
    achievements(): Promise<ResultObject<Paths.ResourcesGuildsAchievements.Get.Responses.$200, [
        "success",
        "lastUpdated"
    ]>>;
    /**
     * Retrieve a list of permissions that a Hypixel guild master can use.
     * @example
     * ```typescript
     * const permissions = await client.resources.guilds.permissions();
     * ```
     * @category API
     */
    permissions(): Promise<ResultArray<Paths.ResourcesGuildsPermissions.Get.Responses.$200, "permissions">>;
}
